
const initialState = {
    friends:[
        {id:1, name: "Andry"},
        {id:2, name: "Maks"},
        {id:3, name: "Alex"}
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state
};
export default sidebarReducer
